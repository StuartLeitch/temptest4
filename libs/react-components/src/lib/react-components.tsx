import React from 'react';

import './react-components.scss';

/* eslint-disable-next-line */
export interface ReactComponentsProps {}

export const ReactComponents = (props: ReactComponentsProps) => {
  return (
    <div>
      <h1>Welcome to react-components component!</h1>
    </div>
  );
};

export default ReactComponents;
