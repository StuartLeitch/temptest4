// import { join } from 'path';

// const root = join(__dirname);

const config = {
  // rootDir: root,
  // Targets ========================================================
  // serveDir: join(root, '.serve'),
  // distDir: join(root, 'dist'),
  // clientManifestFile: 'manifest.webpack.json',
  // clientStatsFile: 'stats.webpack.json',

  // Source Directory ===============================================
  // srcDir: join(root, 'src', 'app'),
  // srcServerDir: join(root, 'server'),

  // HTML Layout ====================================================
  // srcHtmlLayout: join(root, 'src', 'index.html'),

  // Site Config ====================================================
  siteTitle: 'Airframe',
  siteDescription: 'Default Dashboard ready for Development',
  siteCanonicalUrl: 'http://localhost:4200',
  siteKeywords: 'react dashboard seed bootstrap',
  scssIncludes: []
};

export default config;
