import styled from "styled-components";

import { layout, space, flex } from "styled-system";

export const TotalCharges = styled.div`
  justify-content: space-between;
  align-items: flex-start;
  display: flex;

  ${layout};
  ${space};
  ${flex};
`;
