import { Resolvers } from '@hindawi/invoicing-graphql/graphql/schema';

/**
 * Filter by
 *   journal title (allow multiple selection)
 *   transaction status
 *   invoice status (allow multiple selection)
 *   manuscript customID
 *   invoice reference
 */

export const filtering: Resolvers<any> = {
};
