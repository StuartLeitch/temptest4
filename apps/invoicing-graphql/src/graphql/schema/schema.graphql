enum InvoiceStatus {
  DRAFT
  ACTIVE
  FINAL
}

type Invoice {
  id: String
  dateCreated: String
  dateChanged: String
  vat: Float
  charge: Float
  status: InvoiceStatus
}

input PayerInput {
  type: PayerType
  country: String
  name: String
  billingAddress: String
  organization: String
}

enum PayerType {
  INSTITUTION
  INDIVIDUAL
}

type Payer {
  name: String
  id: String
  country: String
  billingAddress: String
  organization: String
  type: PayerType
}

type Query {
  invoice(id: String): Invoice
  echo(value: String): String
}

type Mutation {
  updateInvoicePayer(payerId: String! payer: PayerInput!): Payer!
  createInvoice(totalAmount: Float): Invoice
  deleteInvoice(id: String!): Boolean
}
