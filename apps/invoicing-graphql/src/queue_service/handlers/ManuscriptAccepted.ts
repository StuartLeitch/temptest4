const MANUSCRIPT_ACCEPTED = 'ManuscriptAccepted';

export const ManuscriptAccepted = {
  event: MANUSCRIPT_ACCEPTED,
  handler: async data => {
    // TODO: Add the usecase to process this data
  }
};
