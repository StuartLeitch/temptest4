const MANUSCRIPT_REJECTED = 'ManuscriptRejected';

export const ManuscriptRejected = {
  event: MANUSCRIPT_REJECTED,
  handler: async data => {
    // TODO: Add the usecase to process this data
  }
};
